# Official Pi Testnet app URL (no trailing slash)
NEXT_PUBLIC_APP_URL=https://youneonwtce7005.pinet.com

# Pi Network sandbox flag for the client SDK (Pi.init sandbox).
# "true"  = Testnet. "false" = Mainnet / production ecosystem.
# This project targets Pi Testnet. Do not auto-switch. Redeploy after any change.
NEXT_PUBLIC_PI_SANDBOX=true

# ---------------------------------------------------------------------------
# Pi Ecosystem vs Pi Develop — do not mix networks or keys
#
# api.minepi.com/v2 is used for BOTH Testnet and Mainnet.
# The Server API Key selects the network. A different pinet.com host
# (e.g. youneonwtce7005 vs another listing) can be a different Pi app —
# each app needs its own matching key.
#
# TESTNET (Pi Develop / Testnet Pi Browser / Testnet ecosystem listing):
#   1. Open Testnet Pi Browser and the Testnet app URL
#   2. Vercel PI_API_KEY = Sandbox Server API Key (Pi Develop → API Keys)
#   3. NEXT_PUBLIC_PI_SANDBOX=true  (Pi.init sandbox:true)
#   4. Pay with Test-π. Same Pi account in the wallet as signed into the app.
#
# PRODUCTION (Pi Ecosystem listing / Mainnet Pi Browser):
#   1. Open Mainnet Pi Browser and the production ecosystem listing
#   2. Vercel PI_API_KEY = Production Server API Key
#   3. NEXT_PUBLIC_PI_SANDBOX=false
#   4. Do not pay with Test-π. Do not use the Sandbox key.
#
# If those are swapped: the wallet can look successful while server complete
# returns 401, and Pi Develop step 10 (developer_completed) stays gray.
# Redeploy on Vercel after any env change.
# ---------------------------------------------------------------------------

# Public Pi Sign-In OAuth client_id (Testnet, implicit flow only — no client_secret).
# This is NOT a payment API key. Set the same name on Vercel (build-time NEXT_PUBLIC_*).
# Redirect URIs are not configured yet in Pi Develop — add them there, e.g.
#   https://youneonwtce7005.pinet.com
#   https://youneon-app.vercel.app
NEXT_PUBLIC_PI_CLIENT_ID=w_xWQmfWJ4F48E4IztcO8W5ST0c6sCWa9q9uARUAtSQ

# Pi Platform Server API Key (server-only). Required for U2A approve + complete.
# Never expose this to the client. Do not commit the real key.
# Testnet: Pi Develop → API Keys → SANDBOX Server API Key
# Mainnet: Pi Develop → API Keys → PRODUCTION Server API Key
# Must match NEXT_PUBLIC_PI_SANDBOX. Set the same name on Vercel.
# Aliases (first non-empty wins): PI_API_KEY, PI_NETWORK_API_KEY, PI_PLATFORM_API_KEY.
PI_API_KEY=
PI_NETWORK_API_KEY=
# PI_PLATFORM_API_KEY=

# Optional Platform API base. Default is the official host (not sandbox.minepi.com —
# that URL is the desktop sandbox UI). Sandbox vs production is the API KEY.
# PI_PLATFORM_API_BASE=https://api.minepi.com/v2

# Pi app identity: NEXT_PUBLIC_PI_CLIENT_ID above is the OAuth client_id from
# Pi Develop → your app. The Vercel / pinet.com URL must be registered on that same app.

# Comma-separated Pi usernames that can open /admin (Sherifjaber is always included in code).
NEXT_PUBLIC_ADMIN_PI_USERNAMES=Sherifjaber

# Firebase Admin SDK (server-only). Required to create/claim promo codes after firestore.rules deny client writes.
# Paste a service account JSON as one line, or use the three fields below.
FIREBASE_SERVICE_ACCOUNT=
FIREBASE_PROJECT_ID=youneon
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Daily.co video rooms (server creates rooms; never commit .env.local).
# Get a key at https://dashboard.daily.co — set DAILY_API_KEY on Vercel/host.
DAILY_API_KEY=
# Your Daily subdomain (full host is youneon.daily.co)
NEXT_PUBLIC_DAILY_DOMAIN=youneon
